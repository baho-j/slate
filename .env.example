# Root pointer only — each app owns its real .env (apps/api/.env, apps/web/.env).
# This documents the shared values the docker-compose Postgres service expects.

POSTGRES_DB=slate
POSTGRES_USER=slate
POSTGRES_PASSWORD=slate
POSTGRES_PORT=5432
